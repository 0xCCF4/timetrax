# Todos

List of todos for this project:
- [ ] split the output interface into a struct layer which can then be rendered to json or human readable output
- [ ] use library for colored terminal output
- [ ] put files into Year/Month folders by default
- [ ] add a quota reapply command to reapply the quota for a given day/timespan, e.g. after changing the quota for a job
- [ ] add duration format to also days, years
- [ ] report allow also relative time
- [ ] list problems #days with open ended activities
- [ ] use tabular crate for output human foramt
- [ ] set quotas for a year, e.g. for vacation days